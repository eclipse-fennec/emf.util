# ⚠️ Deprecated — moved to event.atlas

This project has moved to the
[`eclipse-fennec/event.atlas`](https://github.com/eclipse-fennec/event.atlas) repository.
All further development happens there; this copy is frozen and will be removed in a
future cleanup.

This project was **not moved**: its coverage (atlas-fed EObject registry pipeline into
the mapping facades) is superseded by the event.atlas workspace — the eobject.provider's
plain-JUnit tests and the mapping OSGi tests there. It will be removed together with the
other `sensinact.mapping*` projects.
