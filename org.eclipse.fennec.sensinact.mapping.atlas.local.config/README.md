# ⚠️ Deprecated — moved to event.atlas

This project has moved to the
[`eclipse-fennec/event.atlas`](https://github.com/eclipse-fennec/event.atlas) repository (renamed).
All further development happens there; this copy is frozen and will be removed in a
future cleanup.

New home: `org.eclipse.fennec.event.atlas.mapping.local.config` (configurator
symbolic name changed accordingly; the `required.nsuris` entry now names the event.atlas
mapping nsURI `https://fennec.eclipse.org/event.atlas/mapping/1.0`).
