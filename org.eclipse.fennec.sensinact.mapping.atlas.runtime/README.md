# ⚠️ Deprecated — moved to event.atlas

This project has moved to the
[`eclipse-fennec/event.atlas`](https://github.com/eclipse-fennec/event.atlas) repository (renamed).
All further development happens there; this copy is frozen and will be removed in a
future cleanup.

New home: `org.eclipse.fennec.event.atlas.mapping.runtime` — carries `launch.bndrun`
(the atlas-integrated playground) and additionally `eventatlas.runtime_docker.bndrun`,
the self-contained docker runtime published as `eclipsefennec/event.atlas` /
`ghcr.io/eclipse-fennec/event.atlas`.
